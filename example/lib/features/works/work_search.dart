import 'package:flutter/material.dart';

class WorkSearch extends StatelessWidget {
  const WorkSearch({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Work Search')),
      body: const Center(
        child: Text('Work Search'),
      ),
    );
  }
}
