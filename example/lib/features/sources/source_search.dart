import 'package:flutter/material.dart';

class SourceSearch extends StatelessWidget {
  const SourceSearch({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Source Search')),
      body: const Center(
        child: Text('Source Search'),
      ),
    );
  }
}
